const currencies = [...];

let currsFrom = document.getElementById('https://free.currencyconverterapi.com/api/v5/currencies');
for (const currencies of Object.keys(currsTo)){
	let currsTo = <option value = "${val['id']}">${
		val['currencyName']
}</option>;

currencies.push(currsTo);
}
let result = currencies.join('');